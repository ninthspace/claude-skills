---
topic: <Human-readable subject, title case>
slug: <matches the directory suffix>
started: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
status: active
tags: [<lowercase>, <single-word-where-possible>, <3-6 of them>]
related: []
question: <One sentence. What this session set out to answer.>
---

# <Topic>

## Question

What prompted this, and what would count as an answer. Two or three sentences.

## Summary

The answer as it currently stands, written so it makes sense to someone who reads only
this section, six months from now, with no memory of the session. Revise this on every
continuation rather than appending to it.

## Open threads

- [ ] Something unresolved, phrased as the question that would resolve it
- [ ] Something deliberately deferred, with a note on why

## Session log

| Date | Covered |
|---|---|
| <YYYY-MM-DD> | Initial session — <scope in a few words> |

---

`findings.md` holds the substance, `sources.md` the references, `artifacts/` the files.

<!--
status values:
  active     — still being worked, open threads live
  parked     — paused deliberately; the reason belongs in Open threads
  concluded  — the question is answered; further work starts a new session
-->
