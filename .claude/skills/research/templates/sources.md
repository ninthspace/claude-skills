# Sources — <Topic>

Ordered authoritative first. Mark each `[A]` authoritative (official docs, source code,
specification, primary data) or `[O]` opinionated (blog, forum, talk, anecdote) — the
distinction is the whole point of keeping this file separate.

## <YYYY-MM-DD>

- **[A] <Title>** — <URL>
  What it is, and what it settled. Note the version or date the source itself carries
  where that matters; documentation ages badly and silently.

- **[O] <Title>** — <URL>
  What it claims, and whether anything corroborated it. Say so plainly when a source
  turned out to be wrong or unusable — the next pass should not re-read it hopefully.

- **[A] <Local path>** — `artifacts/<file>`
  Saved locally because <it was behind a block / it changes / the excerpt is long>.
